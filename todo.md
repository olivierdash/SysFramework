# Projet todo

Faire le pullRequest de sprint 0 [10 min]
Framework
    [x] Creer une annotation Controller pour indiquer qu'une classe est un controller [25 min]

    FrontServlet
        attribut:
            [x] List<String> listClass (pour stocker les classes annotes Controller)
            a initialiser dans la methode init [1 min]
Test
    - Creer une classe qui utilise cette annotation Controller [5 min]
    - Creer un bout de code au demarrage de l'application web (Conseil: utiliser Listener) ou au premier appel du FrontServlet [15 min]
    - Dans le web.xml : mettre une variable (pour le nom du package dans lequel on va chercher les controllers) [10 min]

Creer une classe Utility
    Methode :
        Verifier qu'un annotation se trouve au niveau (classe, attribut,Methode) pour tel classe dans tel package [25 min]

PullRequest : Limite Dimanche
